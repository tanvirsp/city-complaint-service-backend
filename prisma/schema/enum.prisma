enum AuthProvider {
  GOOGLE
  CREDENTIAL
}

enum Role {
  ADMIN
  CITIZEN
  STAFF
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum CategoryType {
  COMPLAINT
  SERVICE
}

enum StaffVerificationStatus {
  PENDING
  APPROVED
  REJECTED
}

enum Priority {
  LOW
  MEDIUM
  HIGH
}

enum ComplaintStatus {
  PENDING
  ASSIGNED
  IN_PROGRESS
  RESOLVED
  REJECTED
}

enum ServiceStatus {
  PENDING
  ASSIGNED
  IN_PROGRESS
  RESOLVED
  REJECTED
}
