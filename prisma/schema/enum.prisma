enum AuthProvider {
  GOOGLE
  CREDENTIAL
}

enum Role {
  ADMIN
  CITIZEN
  STAFF
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum CategoryType {
  COMPLAINT
  SERVICE
}

enum StaffVerificationStatus {
  PENDING
  APPROVED
  REJECTED
}

enum Priority {
  LOW
  MEDIUM
  HIGH
}

enum ComplaintStatus {
  PENDING
  ONGING
  COMPLETE
  REJECTED
}
