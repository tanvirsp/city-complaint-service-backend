enum AuthProvider {
  GOOGLE
  CREDENTIAL
}

enum Role {
  ADMIN
  CITIZEN
  STAFF
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum CategoryType {
  COMPLAINT
  SERVICE
}

enum StaffVerificationStatus {
  PENDING
  APPROVED
  REJECTED
}
